org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# Pinned to AGP 8.13 deliberately. Flutter 3.44.1's Gradle plugin has no
# built-in Kotlin support: FlutterPluginUtils force-applies kotlin-android to
# every plugin subproject that declares AGP but not KGP, which AGP 9 rejects
# outright. Under AGP 8 that force-apply is what compiles app_links, and
# file_picker's own isAgp9OrAbove guard applies KGP itself. Do not raise AGP to
# 9 until the Flutter SDK understands android.builtInKotlin.
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
